#include <iostream>

using std::cout;
using std::cin;
using std::endl;

int main(){
    system ( "clear" );

    short *myShortPtr { nullptr };
    int *myIntPtr { nullptr };
    

    fflush ( stdin );
    cin.get();
    return 0;
}
